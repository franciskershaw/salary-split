{
  "buildCommand": "cd ../.. && pnpm install && pnpm run build:frontend",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}